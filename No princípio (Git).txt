git add "o nome do arquivo" = para adicionar

git add. = para adicionar varios

git status = para ver os arquivos e status

git commit -m "commit inicial" 

 git config --global user.email "you@example.com"
  git config --global user.name "Your Name"
git push
git remote add origin "git address"
git reflog = atualizações
git reset --hard 'o id do reflog'
git branch =mostra os branch
git branch staging = cria um branch
git checkout staging = troca a branch usada
git merge (nome do branch add)
git pull = para trazer atualizaçoes do servidor
